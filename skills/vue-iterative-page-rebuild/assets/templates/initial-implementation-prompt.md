你是资深 Vue 前端工程师。

任务：根据 `target.png` 与 `layout.json` 实现页面。

要求：
1. 先还原结构与相对位置，再修视觉细节。
2. 使用 Vue SFC（`template` + `script setup` + `style`）。
3. 组件命名与 `layout.json` 保持一致。
4. 不引入无关依赖。
5. 输出：变更文件清单 + 关键实现说明。
